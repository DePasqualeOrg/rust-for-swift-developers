# Project Agent Rules

- Never run `npm` commands on this machine for this repository.
- Use the VS Code devcontainer workflow for dependency installation, builds, previews, and other tasks that would normally rely on `npm`.
